apiVersion: azuredevops.upbound.io/v1alpha1
kind: Environment
metadata:
  annotations:
    crossplane.io/external-name: "20"
  name: example-04
spec:
  deletionPolicy: Orphan
  managementPolicy: FullControl
  forProvider:
    name: example-04
    description: Example Environment desc
    projectIdRef:
      name: example-04
